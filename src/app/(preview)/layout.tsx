/**
 * Layout pour le mode prévisualisation
 * Pas de menu ni de navigation - juste le contenu
 */
export default function PreviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
